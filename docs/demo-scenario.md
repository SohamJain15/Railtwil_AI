# Demo scenario

The Vasai freight bottleneck is a controlled synthetic scenario. It injects one eight-minute freight delay. SimPy movement, propagation, prediction, conflicts, what-if evaluation, optimization, and safety validation calculate all subsequent outputs. `Load Demo` and `Reset Demo` reconstruct the deterministic initial twin.
