# Conflict detection

The detector consumes actual or projected resource intervals. Overlapping incompatible block/platform intervals and headways below the configured minimum create typed conflicts. Outputs contain resource, trains, severity, time, and probability. Limitations include simplified route compatibility and no connection to official signalling logic.
